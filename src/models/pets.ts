/* eslint-disable no-unused-vars */
/* export type PetsStructure = {
  kind: string;
  name: string;
  age: string;
};

export class Pets implements PetsStructure {
  static generatePet() {
    return Math.trunc(Math.random() * 1_000_000_000_000).toString();
  }

  kind: string;
  constructor(public title: string, public owner: string) {
    this.kind = Pets.generatePet();
  }
}
*/
